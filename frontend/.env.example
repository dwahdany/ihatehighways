VITE_GOOGLE_MAPS_API_KEY=

# Optional backend base URL (e.g. http://localhost:8000) for deployments that
# serve the frontend without the /api proxy; the backend's IHH_CORS_ORIGINS
# must allow this origin. Leave empty for same-origin /api (Vite proxy in dev).
VITE_API_BASE=
